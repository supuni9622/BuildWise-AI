"""BuildWise v2 — CrewAI-native product consulting backend.

Rebuilt from scratch per the specifications in ``prds/``. This package does
not import from ``buildwise`` (the previous implementation).
"""
